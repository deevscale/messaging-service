package com.deevscale.messaging;

public class Processor {

    //Kafka stream

    //Pattern pattern = pattern.parse("\\W" + UNICODE_CHAR
    //KStream<String,int> = KafkaInput.Stream()
    //.mapFlattenValues(value -> pattern.parse(value.toLowercase())
    //.groupBy((key,value) -> value)
    //.count()

    //return KafkaOutput(KStream, koutputsomething)
}
